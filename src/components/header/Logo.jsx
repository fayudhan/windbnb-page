import { logo } from "../../assets";

const Logo = () => {
  return (
    <a href="#" className="esm:px-0 lg:px-4 py-5">
      <img src={logo} alt={logo} className="w-20" />
    </a>
  );
};

export default Logo;
